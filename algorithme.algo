ALGORITHM 
VAR
    i,j,k:INTEGER;
    m:string;
    

BEGIN
    REPEAT
        Write("saisie une chaine de caractére ")
        read(m)
    UNTIL (m[length (m)-1]== ".")

    i:=0;
    j:=0;
    k:=0;
    REPEAT
    IF (m[i] in "aeyuioAEYUIO") THEN
        j:=j+1
    END_IF
    IF (m[j]==" ") THEN
        k:= k+1
    END_IF
        i:=i+1
        
    UNTIL (ch[m]== ".")


    Write("nbre de carac:",i)
    Write("nbre de voyelle:",j)
    Write("nbre de mots:",k+1)

END